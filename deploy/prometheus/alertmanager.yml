global:
  resolve_timeout: 5m
  smtp_smarthost: "smtp.gmail.com:587"
  smtp_from: "radoslav.s.slavov@gmail.com"
  smtp_auth_username: "radoslav.s.slavov@gmail.com"
  smtp_auth_password: "nhjo vapi eady zrmg"
  smtp_require_tls: true

route:
  receiver: "email-notifications"
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: "email-notifications"
    email_configs:
      - to: "radoslav.s.slavov@gmail.com"
        send_resolved: true
