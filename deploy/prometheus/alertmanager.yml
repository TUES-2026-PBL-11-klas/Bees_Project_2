global:
  resolve_timeout: 5m

route:
  receiver: "webhook-notifications"
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: "webhook-notifications"
    webhook_configs:
      - url: "http://host.docker.internal:8000/webhook" # Example target webhook URL
