apiVersion: apps/v1
kind: Deployment
metadata:
  name: clearwake-api
  labels:
    app: clearwake-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: clearwake-api
  template:
    metadata:
      labels:
        app: clearwake-api
    spec:
      containers:
        - name: api
          image: ghcr.io/tues-2026-pbl-11-klas/bees_project_2:f77030cc97b8581c9054704fb021e87626aa247c
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: clearwake-config
            - secretRef:
                name: clearwake-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/v1/zones/
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/v1/zones/
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
